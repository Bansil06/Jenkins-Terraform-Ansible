pipeline {
    agent any

    environment {
        AWS_ACCESS_KEY_ID     = credentials('aws-creds')
        AWS_SECRET_ACCESS_KEY = credentials('aws-creds')
        AWS_DEFAULT_REGION    = 'ap-south-1'
    }

    stages {

        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }

        stage('Terraform Init') {
            steps {
                sh 'terraform init'
            }
        }

        stage('Terraform Apply') {
            steps {
                sh 'terraform apply -auto-approve -var="key_name=key"'
            }
        }

stage('Commit static_inventory file into GitHub') {
    steps {
        withCredentials([string(credentialsId: 'github-pat', variable: 'GIT_TOKEN')]) {
            sh '''
              git fetch origin main
              git checkout -B main

              git config user.email "patelbansi@gmail.com"
              git config user.name "Bansil06"

              if [ -f static_inventory ]; then
                  git add static_inventory
                  git diff-index --quiet HEAD || git commit -m "static_inventory added by Jenkins"
                  # Use username + token for HTTPS push
                  git push https://Bansil06:${GIT_TOKEN}@github.com/Bansil06/Jenkins-Terraform-Ansible.git main
              else
                  echo "static_inventory file not found, skipping commit"
              fi
            '''
        }
    }
}

stage('Ansible - Install Docker') {
    steps {
        sh '''
        docker run --rm -v $PWD:/ansible -w /ansible williamyeh/ansible:alpine \
            ansible-playbook -i static_inventory playbooks/install_docker.yml --user ubuntu --private-key /path/to/key.pem
        '''
    }
}
    }

    post {
        success {
            echo "Instance ready and configuration completed"
        }
        failure {
            echo "Pipeline failed. Check logs above."
        }
    }
}
